services:
  - type: web
    name: portfolio-2025
    env: python
    pythonVersion: 3.9
    buildCommand: "python -m venv venv && source venv/bin/activate && pip install -r requirements.txt && python manage.py collectstatic --noinput"
    startCommand: "gunicorn backend.wsgi:application"