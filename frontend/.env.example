# Base URL for the Django REST API (with or without trailing slash — the app normalizes it)
# Local dev:
VITE_API_BASE_URL=http://localhost:8000/api/

# Production:
# VITE_API_BASE_URL=https://api.miguel-villarreal.com/api/
